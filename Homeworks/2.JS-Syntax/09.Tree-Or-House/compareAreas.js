(function () {
    'use strict';

    function compareAreas(number){

        //TODO... write the program logic

        return '';
    }

    //TODO: crete arrays with random numbers to test the program
    var testResult,
        randomNumbers = [];

    testResult = compareAreas(randomNumbers);

    console.log(randomNumbers + ' -> bigger area has the ' + testResult);

}());